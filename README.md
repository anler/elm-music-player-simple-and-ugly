Installation with: `elm package install`.
Run with: `elm reactor` and navigate to `http://localhost:8000` and you should see something like this:

![](./elm-reactor-screenshot.png)

then click `Main.elm`


